ANGULAR ANAVNZADO

	plantillas para angular => wrap pixel

	--crea tag en git
		git tag -a <NOMBRE_TAG> -m "version inicial"
		git push --tag
		--delete tag
		git tag -d <NOMBRE_TAG> 
		
	comandos angular/cli

		ng g c carpeta/NOMBRE
		ng g c carpeta/NOMBRE --spec=false -- no genera archivo spec
		ng g s carpeta/NOMBRE_SERVICE
		ng g g carpeta/NOMBRE
		ng g c carpeta/NOMBRE -m="<OBJETO_COMPONENTE_DONDE_AGREGAR>" -spec=false --is
		
		guards  => Autenticacion de usuario
			Los protectores de ruta de Angular son interfaces que pueden decirle al enrutador si debe o no permitir la navegación a una ruta solicitada.
			Toman esta decisión buscando un valor de retorno verdadero o falso de una clase que implementa la interfaz de protección dada.		
			Hay cinco tipos diferentes de guardias y cada uno de ellos se llama en una secuencia particular. El comportamiento del enrutador se modifica de manera 
			diferente dependiendo de qué guardia se use. Los guardias son:
			- CanActivate
			- CanActivateChild
			- CanDeactivate
			- CanLoad
			- Resolve
			
	Trabajaremos con @Outputs, @Inputs y @ViewChild
		- Aprenderemos como utilizar atributos personalizados
		- Crearemos un componente re utilizable con una funcionalidad en especifico
		- Aprenderemos a tener referencias a elementos HTML
		- Tips de JavaScript puro: colocar el foco en elementos
		- Uso de gráficas como componentes personalizados
		

	ng2-charts (https://valor-software.com/ng2-charts/)
		npm install ng2-charts --save
		npm install chart.js --save
		
	
	Crear snippets
	
	Trabajar con observables y promesas
	
		trabajar con peticiones asincronas, esperar una respuesta que no llega de forma instantanea.
		
		http://reactivex.io/documentation/operators.html
		
	
	meta tags
	
	Formularios reactivos(Forms reactive)
	--plugin para mostrar mensajes de error
	
	
	sweetalert2
		npm install --save sweetalert
		
	
BACKEND
	
	--BACKEND NODEJS expressjs
	
		--instal mongoose
			npm install mongoose
		--install nodemoon
				npm install -g nodemon
				
		--para tratar body en el request
		 npm install body-parser --save
		 
		--mongoose-unique-validator
			npm install --save mongoose-unique-validator
			
		--bcrypt.js	 (para ecncriptar claves)
			npm install bcryptjs --save
			
		--node-jsonwebtoken
			npm install jsonwebtoken --save
			
		--express-fileupload: Para trabjar con carga de imaganes
			express-fileupload
			npm install --save express-fileupload --save
		
		--install google auth
			npm install google-auth-library@0.12.0 --save
			
		
	 --express cors
		
	
	